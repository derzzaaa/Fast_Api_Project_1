# Список задач / TODO List

### Безопасность и логирование
- [-] **Настроить логирование (Logger)**
  * Подключить стандартный модуль `logging` (или библиотеку `loguru`).
  * Настроить вывод логов в консоль разработчика и запись критических ошибок в файл.
  * Заменить возвращение сырых ошибок базы данных (`str(e)`) клиенту на запись этих деталей в лог, а пользователю возвращать общую безопасную ошибку `500 Internal Server Error`.

### Аутентификация и пользователи
- [/] **Реализовать регистрацию и аутентификацию пользователей**
  * [-] Создать систему пользователей в памяти (хеширование bcrypt, JWT-токены, эндпоинты `/register` и `/login`).
  * [+] Интегрировать текущего пользователя в процесс создания заказа.
  * [ ] Рефакторинг эндпоинта создания заказа: имя пользователя берется из `current_user` токена.
  * [+] Перевести базу данных пользователей и заказов с имитации (in-memory) на реальные таблицы PostgreSQL (раскомментировать строки в `database.py` и `crud.py`).

### Работа с заказами
- [ ] **Реализовать уникальные номера заказов в течение дня**
  * Сейчас номер заказа (`order_number`) генерируется случайно через `random.randint`.
  * В будущем нужно подключить кэш (например, Redis) для хранения и инкремента последовательных номеров заказов (№1, №2, №3...).
  * Реализовать сброс счетчика номеров в кэше в полночь каждые сутки, чтобы номера заказов за один день не дублировались и начинались с 1 каждый день.
